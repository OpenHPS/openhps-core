import { DataFrame } from "../../data";
import { Node } from "../../Node";

export class WorkerPoolNode<InOut extends DataFrame> extends Node<InOut, InOut> {

    constructor() {
        super();
    }

    
}
