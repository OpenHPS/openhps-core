import { expect } from 'chai';
import 'mocha';

describe('node', () => {
    describe('inflate', () => {
        it('should inflate the frame', () => {

        });
    });

    describe('deflate', () => {
        it('should deflate the frame', () => {

        });
    });
});