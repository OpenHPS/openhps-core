import { expect } from 'chai';
import 'mocha';
import { DataFrame, DataSerializer, Model, ModelBuilder, CallbackSinkNode, DataObject } from '../../../../src';

describe('node', () => {
    describe('sma filter', () => {
        
    });
});